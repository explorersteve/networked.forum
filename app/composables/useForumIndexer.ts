/**
 * @deprecated Use `useForumPosts` — Convex is the source of truth for Recent posts.
 * Kept as a thin alias so existing imports keep working during the migration.
 */
export { useForumPosts as useForumIndexer } from './useForumPosts'
